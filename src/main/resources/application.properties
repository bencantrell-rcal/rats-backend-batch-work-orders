spring.application.name=rats-backend-batch-work-orders

# These are only used locally, since the config map overrides these values when deployed
spring.datasource.read.url=jdbc:mysql://127.0.0.1:3306/rats?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.read.username=devuser
spring.datasource.read.password=Rcal_1995!

spring.datasource.write.url=jdbc:mysql://127.0.0.1:3306/rats?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.write.username=devuser
spring.datasource.write.password=Rcal_1995!

# --- NEW: use unified naming ---
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true